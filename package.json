{
  "name": "challenge-10",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-db": "psql -U postgres -f schema.sql",
    "seeds-db": "psql -U postgres -f seeds.sql",
    "start": "node index.js",
    "init": "npm run setup-db && npm run seeds-db && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "inquirer": "^12.0.1",
    "pg": "^8.13.1"
  }
}
